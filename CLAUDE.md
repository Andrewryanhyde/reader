Repository guidance for coding agents:

- Do not deploy or publish changes without maintainer approval.
- Keep `README.md`, `.env.example`, and `SECURITY.md` in sync when changing environment variables, external integrations, or API behavior.
- Prefer tests for changes that affect auth, request validation, alignment logic, or persisted reading data.
